- view only mode or lock mode
  1. add view only mode in case the folder tree and the editor being touched accidentcally.
  2. this mode should be default and each project would have it own mode. 
  3. there should be a toggle on folder tree it is locked all the CRUD button would be gone

- auto focus on tab
  1. when hit any file on folder tree the tab label on editor should be scrolled to where it is automatically

- ui layout
  1. descrese the hight of the top tool-area on both folder tree and editor to save more space, vertically.